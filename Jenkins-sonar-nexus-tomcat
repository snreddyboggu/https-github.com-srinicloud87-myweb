pipeline{
    agent { label 'slave' }
     tools {
        maven 'Maven'
    }
    
    environment{
        PATH = "/usr/share/apache-maven"
    }
    stages{
        stage("Git Checkout"){
            steps{
               git credentialsId: 'snreddyboggu', url: 'https://github.com/snreddyboggu/https-github.com-srinicloud87-myweb.git'
            }
        }
        
        
       

    }
}
